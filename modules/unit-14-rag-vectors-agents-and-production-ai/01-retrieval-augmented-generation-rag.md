# Week 14 -- RAG, Vectors, Agents and Production AI

## Vector databases -- storing embeddings and enabling similarity search at scale

## The RAG retrieval pipeline -- query -> embed -> similarity search -> top-k -> inject into prompt

## Why RAG reduces hallucination -- AI answers from retrieved evidence, not from memory

## When to use RAG -- use it when the answer depends on data the model was not trained on

