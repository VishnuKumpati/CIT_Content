# Week 12 -- Python Foundations II + Calling an LLM

## What an API is -- a door into another system; request and response

## The Anthropic API -- model, messages array, system prompt, user prompt, authentication

## Making the first API call -- install the library, write the call, run in Colab

## Parsing the JSON response -- extracting the text you need

## Handling API errors -- what to do when the call fails

## Every API call is a design decision -- latency, cost, and reliability

